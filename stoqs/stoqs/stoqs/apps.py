# TODO: this file is not in the original stoqs
# this is for cookiecutter?

from django.apps import AppConfig


class StoqsappConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "stoqs.stoqs"
